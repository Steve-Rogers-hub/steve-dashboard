services:
  - type: web
    name: steve-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run steve_dashboard.py --server.port $PORT --server.address 0.0.0.0
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9